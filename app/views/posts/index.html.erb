
<% @posts.each do |post| %>
  <p>
    <%= post.content %>
    <%= link_to("編集", "/posts/#{post.id}/edit")%>
    <%= link_to("削除", "/posts/#{post.id}/destroy", data: { "turbo-method": :post }) %>
  <p>
<% end %>
